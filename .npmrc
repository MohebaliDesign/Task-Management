# The machine's global npm registry (cache.algonet.ir) was unreachable during
# development (404 on all packages). Pin this project to the public npm registry
# so installs are reproducible. Change if an internal mirror becomes available.
registry=https://registry.npmjs.org/
legacy-peer-deps=true
